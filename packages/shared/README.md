# covenant-shared

Shared types, chain config, EIP-712 helpers, and contract ABIs for [COVENANT](https://github.com/mohamedwael201193/COVENANT).

Used by `covenant-skill` and `covenant-mcp`. Install the MCP server directly:

```bash
npx covenant-mcp
```
